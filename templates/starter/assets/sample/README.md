# Sample assets

These watercolor assets are Studio-generated examples packaged only to keep the starter runnable before a new theme's production assets exist.

Replace every file in this folder and update `config/world.json` for production. The generic runtime does not reference these filenames directly.
